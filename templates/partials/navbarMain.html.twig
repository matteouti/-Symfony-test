<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <div class="container">
        <a class="navbar-brand" href="#">Ma Boutique</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_product_index") }}"><i class="bi bi-bag-plus"> </i> Products</a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_user_index") }}"><i class="bi bi-emoji-smile"> </i> Users</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_orders_details") }}"> <i class="bi bi-list-check"> </i> Orders</a>
                </li>
                {% endif %}

            </ul>
            <!-- Button aligned to the right -->
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    Logged in as: <strong>{{ app.user.userIdentifier }}</strong> |
                    <a href="{{ path('app_logout') }}" class="btn btn-danger">
                        <i class="bi bi-box-arrow-right"></i> Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<style>
    .navbar-nav .nav-link {
        color: #000; /* Default link color */
        transition: color 0.3s ease;
    }
    .navbar-nav .nav-link:hover {
        color: #fff; /* Text color on hover */
        background-color: #007bff; /* Blue background on hover */
        border-radius: 5px; /* Optional: Rounded corners for the background */
        padding: 5px 10px; /* Optional: Add padding to improve spacing */
        text-decoration: none; /* Remove underline (optional) */
    }
</style>
